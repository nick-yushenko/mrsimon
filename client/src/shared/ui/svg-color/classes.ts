import { createClasses } from "@/shared/theme/createClasses";

// TODO обдумать нужено ли вообще добавлять классы элементам (в рамках всего проекта)
export const svgColorClasses = {
  root: createClasses("svg__color__root"),
};
